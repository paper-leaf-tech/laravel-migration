<?php

namespace App\Jobs\Migration;

use PaperleafTech\LaravelMigration\Jobs\BaseMigrationJob;
use PaperleafTech\LaravelMigration\Interfaces\MigrationJobInterface;

class DummyJob extends BaseMigrationJob implements MigrationJobInterface
{
    /**
     * Handle the migration job for the given item.
     *
     * Columns arrive aliased with the initials of their source table, so a
     * FNAME column on the USERS table is available as $item->U_FNAME.
     *
     * @param object $item
     * @return void
     */
    public function handleItem(object $item): void
    {
        // Rules to either skip or migrate record.
        // Parse data into desired format for storage in Laravel model.

        $data = [
            // The record's data to be stored in the model.
        ];

        // TODO: replace DummyModel with the model this table migrates into,
        // then uncomment. saveQuietly() keeps model observers from firing.
        //
        // $record = new \App\Models\DummyModel();
        // $record->fill($data);
        // $record->saveQuietly();
    }

    /**
     * Handle the whole chunk at once.
     *
     * Override this instead of handleItem() when the destination writes can be
     * batched. One insert of 500 rows instead of 500 individual saves is
     * usually the single largest speed-up available to a migration job.
     *
     * insertAndMap() returns the new ids keyed by the legacy id, so a child
     * level can be built in memory and written in one statement too.
     *
     * @param iterable<object> $items
     * @return void
     */
    // public function handleChunk(iterable $items): void
    // {
    //     $rows = collect($items)->map(fn (object $item): array => [
    //         'legacy_id' => $item->D_ID,
    //         // ...
    //     ])->all();
    //
    //     $ids = $this->writer()->insertAndMap('dummy_models', $rows, 'legacy_id');
    //
    //     $this->writer()->insert('dummy_model_lines', collect($items)->map(
    //         fn (object $item): array => [
    //             'dummy_model_id' => $ids[$item->D_ID],
    //             // ...
    //         ]
    //     )->all());
    // }
}
